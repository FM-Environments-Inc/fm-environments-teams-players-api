query region($name: String!) {
  region(name: $name) {
    name
  }
}

query regions {
  regions {
    name
  }
}

query countries {
  countries {
    name
  }
}

query country($name: String!) {
  country(name: $name) {
    name
  }
}
